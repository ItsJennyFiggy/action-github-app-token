# Local Environment Configurations
# Copy this file to .env for local testing.
# Composite action inputs are passed to scripts as INPUT_<NAME> environment vars.

# The GitHub App slug — resolves /itsjennyfiggy/global/<app>_github_app_{client_id,private_key}
INPUT_APP=figgy_release
INPUT_AWS_REGION=us-west-2
